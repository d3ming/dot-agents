✦ The error happens because your package.json explicitly sets "packageManager": "pnpm@8.15.0", but
  yarn (likely invoked by a shell plugin, prompt theme, or hook on reload) is detecting this mismatch
  and complaining. Since corepack is not enabled on your system, it cannot automatically handle or
  silence this check.

  Your pnpm is installed via Homebrew (/opt/homebrew/bin/pnpm), which conflicts with installing
  corepack via npm.

  I can remove the packageManager field from package.json to stop this error, as you already have the
  correct pnpm version installed.

  Shall I remove the packageManager field?

╭───────────────────────────────────────────────────────────────────────────────────────────────────╮
│ ✓  Edit package.json:   },... =>   },...                                                          │
│                                                                                                   │
│ 16     "engines": {                                                                               │
│ 17       "node": ">=18.0.0",                                                                      │
│ 18       "pnpm": ">=8.0.0"                                                                        │
│ 19 -   },                                                                                         │
│ 20 -   "packageManager": "pnpm@8.15.0"                                                            │
│ 19     }                                                                                          │
│ 20 + }                                                                                            │
╰───────────────────────────────────────────────────────────────────────────────────────────────────╯
✦ I've removed the packageManager field from package.json. This should resolve the error when you
  reload your shell or open a new terminal in this directory.

  You can verify by running reload (if it's an alias) or simply opening a new tab.